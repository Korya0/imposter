# قائمة المهام للتنفيذ الكامل: ميزة إدارة أسماء اللاعبين وتبسيط الإعدادات

بما يتوافق مع قواعد الهندسة القياسية (`CLAUDE.md`) وجماليات الأداء العالي والتجاوب (`Responsive`):

## 1. تعديل الـ State Management (التحكم في الحالة)
- [x] إضافة قائمة الأسماء `playerNames` إلى `GameSetupState` وفئاتها الفرعية في `game_setup_state.dart`
- [x] تعديل `GameSetupCubit` في `game_setup_cubit.dart` لتضمين:
  - [x] تحديث `updateSettings` لمزامنة قائمة الأسماء والعدد وتحديث الحد الأقصى للجواسيس.
  - [x] ميثود `updatePlayerName(int index, String newName)` لتعديل اسم أي لاعب بالـ Index.
  - [x] ميثود `addPlayer()` لإضافة لاعب جديد باسم افتراضي وزيادة العدد.
  - [x] ميثود `removePlayer()` لحذف آخر لاعب (مع التحقق ألا يقل عن 3 لاعبين).
- [x] تعديل `GameSessionCubit` في `game_session_cubit.dart`:
  - [x] تعديل `startGame` لتخزين واستقبال قائمة أسماء اللاعبين `playerNames`.
  - [x] تحديث ميثود `prepareAnotherRound` لتمرير قائمة الأسماء المخزنة.

## 2. إعادة تصميم واجهة الإعدادات (`GameSettingsView` والـ Widgets التابعة لها)
- [x] تصميم ويدجت صف إعدادات الجواسيس والوقت المدمج (توفير مساحة رأسية وعرضهما متجاورين بشكل متجاوب `Responsive Row`).
- [x] بناء ويدجت إدارة قائمة اللاعبين المخصصة:
  - [x] تصميم سطر لكل لاعب يشمل (حاوية رقم اللاعب مرسومة، حقل نصي لإدخال الاسم، وأيقونة قلم للتعديل).
  - [x] أزرار الإضافة والحذف في الأسفل، مع تعطيل زر الحذف عند الوصول للحد الأدنى (3 لاعبين).
  - [x] استخدام الوجت المشتركة `AppGap` للتباعد والتنظيم المناسب للأداء الاستثنائي.
  - [x] عدم تجاوز الويدجت الواحدة لـ 60 سطرًا برمجيًا تماشيًا مع قواعد `CLAUDE.md`.
  - [x] التحقق التام من عدم استخدام أي أرقام ثابتة (Hardcoded) للأبعاد والخطوط والمسافات واستبدالها بالامتدادات التجاوبية (مثال: `context.p`, `context.w`, `context.h`, `context.f`, `context.s`).

## 3. ربط شاشات اللعب وعرض الأسماء
- [x] تعديل `GameViewContent` لتمرير اسم اللاعب الفعلي المستخرج من `GameSessionCubit` لكل دور فحص.
- [x] تعديل `ScanningPhaseWidget` لعرض الاسم المخصص للاعب بدلاً من "Player X" الافتراضية.

## 4. الفحص وضمان جودة الكود
- [x] فحص سلامة البناء (Build) وعدم وجود أي تحذيرات لـ `flutter analyze`.
- [x] اختبار تفاعلية الواجهة وتوافقها مع الأجهزة المختلفة.
